// Crea una función saludar(nombre = "Invitado") que use un parámetro con valor por defecto.
function saludar(nombre = "Invitado") {
console.log(`Bienvenido, ${nombre}!`);
}

saludar();
saludar("Mario");
