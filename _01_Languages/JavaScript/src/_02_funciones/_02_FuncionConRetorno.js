// Crea una función llamada cuadrado(num) que devuelva el cuadrado de un número.
function cuadrado(num) {
  return num * num;
}

let resultado = cuadrado(4);
console.log(`El cuadrado es: ${resultado}`);
