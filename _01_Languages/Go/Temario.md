
---

## 🧭 **Temario.md**

```markdown
# 🧭 Temario — Lenguaje Go

---

## 🟢 Nivel Básico

1. **Introducción y configuración del entorno**
   - Instalación de Go y uso de `go run` / `go build`.
   - Estructura de un archivo `.go`.

2. **Tipos de datos y variables**
   - Tipos básicos: `int`, `float64`, `string`, `bool`.
   - Declaración corta (`:=`) y larga (`var`).

3. **Estructuras de control**
   - Condicionales (`if`, `else`, `switch`).
   - Bucles con `for`.

4. **Entrada y salida**
   - Uso de `fmt.Print` / `fmt.Scan`.

---

## 🟡 Nivel Intermedio

5. **Funciones**
   - Parámetros, retorno múltiple y funciones anónimas.
   - Uso de `defer`.

6. **Colecciones**
   - Arrays, slices y mapas.
   - Iteración con `range`.

7. **Estructuras y métodos**
   - Declaración de `struct`.
   - Métodos asociados y receptores (`receiver functions`).

8. **Paquetes y módulos**
   - `package main`, `import`, `go mod init`.

---

## 🔴 Nivel Avanzado

9. **Concurrencia**
   - Uso de *goroutines*.
   - Comunicación con *channels*.

10. **Manejo de errores**
    - Uso de `error` y `panic`.

11. **APIs con Go**
    - Uso del paquete `net/http`.
    - Endpoints, JSON y `http.HandleFunc`.

12. **Proyecto final**
    - API REST con endpoints básicos y goroutines.
