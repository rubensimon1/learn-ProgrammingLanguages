/*
 * 🧩 Enunciado:
 * Crea un set de números y añade elementos repetidos.
 * Observa que los duplicados no se guardan.
 */

fun main() {
    val numeros = mutableSetOf(1, 2, 3, 2, 1, 4)
    println("Set de números sin duplicados: $numeros")
}
